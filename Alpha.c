#include<stdio.h>
int main()
{
    int character;
    printf("Enter the character:");
        scanf("%ch",&character);
    if(('A'&&'Z')||('a'&&'z'))
    {
        printf("Alphabet");
        
    }
    else
    {
        printf("not");
        return 0;
    }
}
